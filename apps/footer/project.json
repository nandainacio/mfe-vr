{
  "name": "footer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/footer/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "preview": {
      "command": "vite preview --port 4203 --strictPort --host localhost",
      "options": {
        "cwd": "apps/footer"
      }
    },
    "test": {
      "command": "vitest run --environment jsdom",
      "options": {
        "cwd": "apps/footer"
      }
    }
  }
}